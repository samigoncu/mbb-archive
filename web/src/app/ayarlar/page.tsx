import { SettingsManagerView } from "@/features/settings/components/settings-manager-view";

export const metadata = {
  title: "Sistem Ayarları",
};

export default function AyarlarPage() {
  return <SettingsManagerView />;
}
