# Documents UI Feature

Bu klasör Documents bounded context'in UI feature'ları içindir.

Planlanan yapı:

```text
documents/
├── api/
├── components/
├── hooks/
├── model/
└── screens/
```

Business rule UI içine kopyalanmaz.
Authorization kararı backend policy'nin alternatifi değildir.
