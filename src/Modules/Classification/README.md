# Classification Bounded Context

Implemented in v0.8. Owns file plans, dynamic metadata schemas, document classifications and metadata sets. It does not own retention/disposition; Retention will be a separate bounded context.
