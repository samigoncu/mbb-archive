# ADR-0017 — BPMN-aligned workflow subset

**Status:** Accepted

v1.1 is not advertised as a full BPMN engine. It implements a controlled
execution subset and rejects arbitrary executable expressions.
